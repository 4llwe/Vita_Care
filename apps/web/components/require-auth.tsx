"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{fetchMe}from"../lib/session";export function RequireAuth({children}:{children:React.ReactNode}){const r=useRouter(),[ok,setOk]=useState(false);useEffect(()=>{fetchMe().then(()=>setOk(true)).catch(()=>r.replace("/login"));},[r]);return ok?<>{children}</>:<div className="p-10 text-center text-sm text-slate-400">Memeriksa sesi aman…</div>;}
