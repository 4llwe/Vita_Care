# Reviewed squashed baseline candidate — Phase 0

## Scope

- Source branch: `phase0/staging-evidence-v2`
- Source commit: `4decdb8798747b506cb0e9849269d85ca0447e84`
- Generated by the successful `isolated-verification` job with Prisma 5.22.
- Baseline preview SHA-256: `c6dfd4da0abdfc782742ea4a28d9a04c44748054281daee46374f1d4f87207a3`
- Target: a fresh, isolated staging database only.
- Production deployment, production reconciliation, and production writes remain blocked.

## Review result

The generated baseline contains:

- 21 `CREATE TYPE` statements matching all Prisma enums;
- 44 `CREATE TABLE` statements matching all Prisma models;
- 30 unique indexes, 26 non-unique indexes, and 49 foreign keys;
- no `DROP`, `TRUNCATE`, `DELETE`, `INSERT`, or `UPDATE` statements.

The former seven migrations cannot follow this baseline because they overlap with
11 tables and 18 indexes already created by it. They are preserved byte-for-byte
under `docs/legacy-prisma-migrations-prebaseline/` for audit, but are no longer
part of Prisma's executable migration path.

Reference data from the former dynamic-menu and official-contact migrations is
retained in `20260921_navigation_reference_data`. No application or patient data
is included in the baseline.

## Required validation before any staging write

1. `sh scripts/create-baseline-migration.sh` regenerates an identical baseline before any post-baseline migrations are added.
2. `sh scripts/check-migrations.sh` reports complete schema coverage.
3. `pnpm exec prisma migrate deploy` succeeds on a disposable PostgreSQL database.
4. A second `pnpm exec prisma migrate deploy` is a no-op.
5. `pnpm exec prisma migrate status` reports the database is up to date.
6. Unit, build, authorization, and Playwright checks remain green.
7. Snapshot or recreate-point for the fresh Neon staging database is confirmed.

Post-baseline schema changes must use additive forward-only migrations. The Phase 1.1
refresh-session migration is intentionally separate from the reviewed Phase 0 baseline;
do not rewrite or drop the baseline to include it.

## Production restriction

Do not point this baseline at production. An existing database requires a
separate schema diff and `_prisma_migrations` reconciliation plan. Only after an
authoritative backup and review may `prisma migrate resolve --applied` be
considered for an existing matching schema.
